<Page x:Class="PROSCANNERCONT.Views.ModuleLiveView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:fa="clr-namespace:FontAwesome.Sharp;assembly=FontAwesome.Sharp"
      Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0">
            <Border DockPanel.Dock="Left" Width="44" Height="44" CornerRadius="10" Margin="0,0,14,0"
                    Background="{DynamicResource SecondaryBackgroundBrush}">
                <fa:IconBlock x:Name="ModuleIcon" Icon="Shield" FontSize="22"
                              Foreground="{DynamicResource AccentBrush}"
                              HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <Button DockPanel.Dock="Right" x:Name="DisconnectBtn" Content="Disconnect" Click="Disconnect_Click"
                    VerticalAlignment="Center" Height="32" Padding="14,0" Cursor="Hand"
                    Background="{DynamicResource SecondaryBackgroundBrush}" Foreground="{DynamicResource TextBrush}"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"/>
            <StackPanel VerticalAlignment="Center">
                <TextBlock x:Name="TitleText" Text="Module" FontSize="20" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextBrush}"/>
                <StackPanel Orientation="Horizontal">
                    <Ellipse x:Name="StatusDot" Width="9" Height="9" VerticalAlignment="Center" Margin="0,0,6,0"
                             Fill="{DynamicResource SuccessBrush}"/>
                    <TextBlock x:Name="StatusText" FontSize="12" Foreground="{DynamicResource SubtleTextBrush}"/>
                </StackPanel>
            </StackPanel>
        </DockPanel>

        <!-- Stat cards -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,18,0,12">
            <Border Background="{DynamicResource SecondaryBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="18,12" Margin="0,0,12,0" Width="160">
                <StackPanel>
                    <TextBlock Text="EVENTS RECEIVED" FontSize="10" Foreground="{DynamicResource SubtleTextBrush}"/>
                    <TextBlock x:Name="EventCount" Text="0" FontSize="26" FontWeight="Bold" Foreground="{DynamicResource TextBrush}"/>
                </StackPanel>
            </Border>
            <Border Background="{DynamicResource SecondaryBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="18,12" Margin="0,0,12,0" Width="220">
                <StackPanel>
                    <TextBlock Text="LAST EVENT" FontSize="10" Foreground="{DynamicResource SubtleTextBrush}"/>
                    <TextBlock x:Name="LastEvent" Text="—" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" TextTrimming="CharacterEllipsis"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Live events -->
        <Border Grid.Row="2" Background="{DynamicResource SecondaryBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8">
            <DataGrid x:Name="EventGrid" AutoGenerateColumns="False" IsReadOnly="True"
                      Background="Transparent" RowBackground="Transparent" BorderThickness="0"
                      Foreground="{DynamicResource TextBrush}" GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column" Margin="6">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Time"     Binding="{Binding Time}"     Width="90"/>
                    <DataGridTextColumn Header="Severity" Binding="{Binding Severity}" Width="90"/>
                    <DataGridTextColumn Header="Event"    Binding="{Binding Name}"     Width="140"/>
                    <DataGridTextColumn Header="Details"  Binding="{Binding Details}"  Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>
