<Window x:Class="PROSCANNERCONT.Views.AddModuleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:fa="clr-namespace:FontAwesome.Sharp;assembly=FontAwesome.Sharp"
        Title="Add Module" Width="760" Height="600"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}"
        WindowStyle="None" AllowsTransparency="True" ResizeMode="NoResize">

    <Window.Resources>
        <Style x:Key="PillButton" TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource OnAccentBrush}"/>
            <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Opacity" Value="0.88"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="b" Property="Background" Value="{DynamicResource BorderBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource SubtleTextBrush}"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="56"/>
            </Grid.RowDefinitions>

            <!-- Title bar -->
            <Border Grid.Row="0" Background="{DynamicResource SecondaryBackgroundBrush}"
                    CornerRadius="10,10,0,0" MouseLeftButtonDown="TitleBar_MouseDown">
                <Grid Margin="16,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <fa:IconBlock Icon="PuzzlePiece" Foreground="{DynamicResource AccentBrush}"
                                      Width="16" Height="16" Margin="0,0,10,0"/>
                        <TextBlock Text="Add Module" Foreground="{DynamicResource TextBrush}"
                                   FontSize="14" FontWeight="SemiBold"/>
                    </StackPanel>
                    <Button HorizontalAlignment="Right" Width="32" Height="32" Click="Close_Click"
                            Background="Transparent" BorderThickness="0" Cursor="Hand" ToolTip="Close">
                        <fa:IconBlock Icon="Xmark" Foreground="{DynamicResource SubtleTextBrush}" FontSize="15"/>
                    </Button>
                </Grid>
            </Border>

            <TextBlock Grid.Row="1" Margin="20,14,20,6"
                       Text="Choose modules to add to your workspace. When you open one, you'll connect to the machine running it by IP and sign in."
                       Foreground="{DynamicResource SubtleTextBrush}" TextWrapping="Wrap" FontSize="12"/>

            <!-- Module cards -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Padding="12,4,12,4">
                <ItemsControl x:Name="CardList">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="222" Height="172" Margin="8" Padding="14" Opacity="{Binding Opacity}"
                                    Background="{DynamicResource SecondaryBackgroundBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <DockPanel Grid.Row="0">
                                        <Border DockPanel.Dock="Right" CornerRadius="4" Padding="6,2"
                                                Background="{DynamicResource BackgroundBrush}" VerticalAlignment="Top">
                                            <TextBlock Text="{Binding Category}" FontSize="10"
                                                       Foreground="{DynamicResource SubtleTextBrush}"/>
                                        </Border>
                                        <fa:IconBlock Icon="{Binding Icon}" FontSize="22"
                                                      Foreground="{DynamicResource AccentBrush}"/>
                                    </DockPanel>
                                    <TextBlock Grid.Row="1" Text="{Binding Name}" Margin="0,10,0,0"
                                               FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}"/>
                                    <TextBlock Grid.Row="2" Text="{Binding Description}" Margin="0,6,0,0"
                                               FontSize="11" TextWrapping="Wrap" Foreground="{DynamicResource SubtleTextBrush}"/>
                                    <Button Grid.Row="3" Style="{StaticResource PillButton}"
                                            Content="{Binding ButtonText}" IsEnabled="{Binding CanAdd}"
                                            Click="AddCard_Click" Tag="{Binding}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Footer -->
            <Border Grid.Row="3" Background="{DynamicResource SecondaryBackgroundBrush}" CornerRadius="0,0,10,10">
                <Button HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,18,0"
                        Width="90" Style="{StaticResource PillButton}" Content="Done" Click="Close_Click"/>
            </Border>
        </Grid>
    </Border>
</Window>
